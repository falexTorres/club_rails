<%= form_for @home do |f| %>
  <% if home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(home.errors.count, "error") %> prohibited this home from being saved:</h2>

      <ul>
      <% home.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<%= f.label :title_1, class: '' %>
	<%= f.text_field :title_1, class: '' %>

	<%= f.label :content_1, class: '' %>
	<%= f.text_area :content_1, class: '' %>

	<%= f.label :hours_thursday, class: '' %>
	<%= f.text_area :hours_thursday, class: '' %>
	
	<%= f.label :hours_friday, class: '' %>
	<%= f.text_area :hours_friday, class: '' %>
			
	<%= f.label :hours_saturday, class: '' %>
	<%= f.text_area :hours_saturday, class: '' %>
			
  <div class="actions">
    <%= f.submit "Save", class: '' %>
  </div>
<% end %>
